# The Smile Hub — Website

Static site, served with GitHub Pages from the repo root.

- `index.html` — homepage (responsive)
- `thank-you.html` — form confirmation page
- `assets/` — images, videos, favicon
- `support.js`, `image-slot.js` — required runtime, keep beside the HTML

Enable Pages: Settings → Pages → Deploy from branch → main / (root).
